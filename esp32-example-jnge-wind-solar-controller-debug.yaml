<<: !include esp32-example-jnge-wind-solar-controller.yaml

logger:
  level: DEBUG
  logs:
    api.service: WARN
    ota: WARN
    wifi: WARN
    sensor: DEBUG

uart:
  id: uart0
  baud_rate: 9600
  tx_pin: ${tx_pin}
  rx_pin: ${rx_pin}
  debug:
    direction: BOTH
